import { PrototypePage } from '@/components/PrototypePage'

export default function Deployment() {
  return <PrototypePage title="Deployment" description="Managed deployment endpoints are not part of the Engine backend." />
}

